# 代办清单门控机制（共享）

> 所有芯片架构 Agent 激活后的第一步行为规范。

## 执行模式选择

> **铁律：仅支持步进模式，禁止连续模式。每步完成后必须暂停等待用户确认。**

| 模式 | 触发条件 | 行为 |
|------|----------|------|
| **步进模式** （默认模式）| 默认，所有场景 | 每步独立执行，完成后暂停等待用户确认 |
| **调试模式** | 用户明确说"调试模式"、"debug mode"、"后台测试" | Agent 后台执行，自动完成全流程，输出评估报告。**仅用于测试 Agent 流程和文档质量** |

**判断规则**：
- 默认使用步进模式，每步暂停等待确认
- 仅当用户明确要求"调试模式"时才使用调试模式
- 调试模式必须使用 `run_in_background: true`
- **非调试模式禁止后台执行，必须逐步与用户问答**

## 启动门控（强制）

> **⚠️ 必须先输出代办清单，否则不得继续。**
> 清单必须标注每个步骤的预期输出物。

### 清单格式

```
## 代办清单（{模式名}）

| # | 步骤 | 预期输出 | 状态 |
|---|------|----------|------|
| 1 | xxx | xxx.md | ⬜ |
| 2 | xxx | xxx | ⬜ |
| 3 | xxx | xxx | ⬜ |
```

## 步进模式（默认模式）

1. 输出清单
2. 获得用户确认清单后，执行第一步
3. **每完成一步必须暂停**，展示进度并等待用户确认
4. 用户确认后才进入下一步

### 步进模式示例

> **用户**：一步步来，先帮我探索 xxx 模块的需求
>
> **Agent**：
> ## 代办清单（步进模式）
> | # | 步骤 | 预期输出 | 状态 |
> |---|------|----------|-----|
> | 1 | 上下文获取 | 约束清单 | A |
> | 2 | 约束收敛 | 收敛后的约束表 | B |
> | 3 | 方案探索(2-3个) | 候选方案+trade-off | C |
> | 4 | 方案确认 | 推荐方案 | D |
> | 5 | 输出收敛文档 | requirement_summary | E |
> | 6 | ADR 记录 | ADR 文档 | F |
>
> 每步独立执行，完成后等您确认再进入下一步。
>
> **开始步骤 1：上下文获取...**
> （完成后）
> **步骤 1 ✅ 已完成**。约束清单如下：...
> 下一步是步骤 2：约束收敛。请确认继续。

## 清单动态调整规则
- 如果某些输入缺失 → 在步骤 1 标注缺失，后续步骤按已有材料执行
- 如果某些步骤不适用 → 从清单中标注"跳过"
- 如果需要多次迭代 → 标注为"进行中"，所有子项完成后才标记 ✅
- 如果发现需要额外步骤 → 在清单中动态添加

## 调试模式

> **调试模式用于测试 Agent 本身的流程完整性、输出文档质量和方案细化程度。**

### 触发条件

用户明确说以下任一关键词时激活：
- "调试模式"
- "debug mode"
- "后台测试"
- "自动测试"

### 行为规则

| 项目 | 调试模式 | 非调试模式（步进模式） |
|------|----------|----------------------|
| 执行方式 | Agent 后台执行（`run_in_background: true`） | Agent 前台执行，逐步与用户问答 |
| 用户交互 | 无需用户确认，自动完成全流程 | 每步暂停等待用户确认 |
| 输出物 | 完整 outputs/ + flow/ + evaluation.md + optimization.md | 完整 outputs/ + flow/（每个 stage 完整 Q&A 记录） |
| 评估 | 自动生成评估报告（按 eva 标准评分） | 不自动生成评估 |
| 适用场景 | 测试 Agent 流程/文档质量、CI 自动化 | 正式需求采集/方案设计 |

### 调试模式输出

调试模式完成后，额外输出：
1. **evaluation.md**：按评分标准逐维度打分（含门控检查）
2. **optimization.md**：扣分项清单 + 优化建议
3. **flow/ 完整记录**：每个 stage 的完整 Q&A（用于回溯分析）

### 调试模式示例

> **用户**：用调试模式测试 PCIe RC 模块
>
> **Agent**：
> ## 代办清单（调试模式）
> | # | 步骤 | 预期输出 | 状态 |
> |---|------|----------|------|
> | 1 | stage0~F 全流程执行 | outputs/ + flow/ | ⬜ |
> | 2 | 评估报告 | evaluation.md | ⬜ |
> | 3 | 优化建议 | optimization.md | ⬜ |
>
> 调试模式启动，Agent 后台执行。完成后自动通知。
>
> （Agent 后台执行，无需用户确认）
> （完成后通知）
> **调试模式完成**：评分 95.0/100 (S)，详见 evaluation.md。

## 关键决策点（无论哪种模式都必须暂停）

以下场景**无论哪种模式**都必须暂停等待用户确认：
1. **方案选择**：多个候选方案需要用户选定（≥2 个方案时）
2. **输入缺失**：关键输入文档缺失，需要用户提供
3. **架构疑问**：发现文档矛盾或不合理之处需要澄清
4. **范围变更**：执行中发现需要超出原定范围的工作

### 调试模式下的关键决策点处理

调试模式下，上述关键决策点不暂停等待用户确认，而是按以下规则自动处理：

| 场景 | 调试模式行为 | 标注 |
|------|-------------|------|
| 方案选择（≥2 个方案） | 选择推荐方案（第一个方案） | `调试模式自动决策-待确认` |
| 输入缺失 | 使用行业默认值 | `调试模式默认值-待确认` |
| 架构疑问 | 按最合理理解继续 | `调试模式假设-待确认` |
| 范围变更 | 在范围内继续，不扩展 | `调试模式范围限制-待确认` |

所有自动决策项在 evaluation.md 的「调试模式自动决策汇总」章节中列出，供用户事后审查。

---

## 质量检查人工确认规则（强制）

> 执行质量检查清单（`quality-checklist-*.md`）时，人工检查项必须逐项与用户确认。

### 规则定义

| 检查类型 | 执行方式 | 确认要求 |
|----------|----------|----------|
| **脚本可查项**（自动化） | Agent 自动执行检查脚本 | 批量执行，汇总报告，**不逐项确认** |
| **人工检查项** | Agent 逐项分析并给出判断 | **每项必须与用户确认**，用户确认后才标记通过 |

### 人工检查项执行流程

```
对每个人工检查项 MC-xx / QC-xx：
  1. Agent 读取相关章节内容
  2. Agent 给出初步判断（通过/不通过/存疑）
  3. Agent 展示判断依据（引用具体文字或表格）
  4. ⏸️ 暂停等待用户确认
  5. 用户确认 → 标记 ✅ / 用户修正 → 更新判断
```

### 确认格式示例

```
**MC-01 数据通路无断点**（人工检查）

📋 检查内容：§5.1 数据流图从输入到输出完整
🔍 检查结果：通过
📝 判断依据：
   - 输入阶段：data_in_if_* → 描述符接收
   - 阶段1：sqinfo 查表（3cycle）
   - 阶段2：dqid 映射
   - 阶段3：链表节点分配
   - 阶段4：SRAM 写入
   - 输出阶段：ram_req_*
   → 从输入到输出完整，无断点

请确认此项是否通过？[通过/修正]
```

### 批量确认优化（用户可选）

用户可选择以下方式加速确认：
- **逐项确认**（默认）：每项暂停等待确认
- **批量预览**：Agent 一次性展示所有人工检查项的判断结果，用户批量确认或逐项修正
- **跳过确认**：用户说"自动通过"时，剩余人工项自动标记通过

### 异常处理

| 情况 | Agent 行为 |
|------|-----------|
| 检查项不适用 | 标注"跳过"并说明原因，等待用户确认 |
| 无法判断 | 标注"存疑"并说明原因，等待用户决策 |
| 用户修正 | 按用户指示更新判断，记录修正原因 |
