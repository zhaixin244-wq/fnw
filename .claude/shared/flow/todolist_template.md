# E 阶段 Todolist（自包含）

> 本文件是 E 阶段递归分解的执行指南，包含执行所需的全部信息。
> Agent 执行时只需读取本文件，无需回溯上下文。
> v6.8：自包含 todolist + 规则重载机制，解决多级递归注意力飘逸问题。

---

## 0. 规则重载与上下文隔离（每级执行前必须执行）

> **铁律：每级子模块必须在独立上下文中执行，确保与 L0 拥有等同的规则注意力。**
> 不执行规则重载则 [TODOLIST-ERROR] 停止。

### 0.1 上下文隔离机制（核心防飘逸方案）

**问题**：LLM 注意力呈 U 型曲线（开头和末尾强，中间弱）。递归时上下文累积导致 Agent 规则被推到中间区域，注意力衰减。

**解决方案**：每个递归层级使用独立 Agent（subagent）执行，拥有全新上下文窗口。

```
L0 主 Agent（上下文：~7K tokens）
├── 执行 L0 stage0~E
├── 生成 L0 todolist
├── 启动 L1 subagent（全新上下文：~7K tokens）
│   ├── 加载：Agent 定义 + L1 todolist + 父模块摘要
│   ├── 执行 L1 stageB+~E
│   ├── 启动 L2 subagent（全新上下文：~7K tokens）
│   │   ├── 加载：Agent 定义 + L2 todolist + 父模块摘要
│   │   └── 执行 L2 stageB+~D → 返回结果
│   └── 接收 L2 结果 → 更新 L1 状态
└── 接收 L1 结果 → 更新 L0 状态 → 进入 F 阶段
```

**关键优势**：每个 subagent 上下文恒定 ~7K tokens，无论递归多深，注意力与 L0 完全等同。

### 0.2 Subagent 输入清单

每个 subagent 启动时，仅传递以下精简信息（不传递父级完整历史）：

| # | 输入 | 来源 | 大小限制 | 说明 |
|---|------|------|----------|------|
| S-01 | Agent 定义 | `.claude/agents/chip-requirement-arch.md` | 完整 | 规则在上下文顶部 |
| S-02 | 本级 todolist | `{name}/todolist.md` | 完整 | 自包含执行指南 |
| S-03 | 父模块摘要 | todolist §2 | ≤500 tokens | 精简的功能/约束/接口 |
| S-04 | 接口契约 | todolist §3 | ≤300 tokens | 仅本模块相关端口 |
| S-05 | 继承约束链 | todolist §4 | ≤500 tokens | 仅影响本模块的约束 |

**总计 ~8.3K tokens**，与 L0 执行时的上下文大小相当。

### 0.3 规则重载检查清单

| # | 检查项 | 验证方法 | 缺失处理 |
|---|--------|----------|----------|
| RC-01 | Agent 定义已加载 | 上下文包含"核心指令" | [TODOLIST-ERROR] |
| RC-02 | 本级 todolist 已加载 | 上下文包含"父模块上下文摘要" | [TODOLIST-ERROR] |
| RC-03 | 接口契约已加载 | 上下文包含"本模块接口契约" | [TODOLIST-ERROR] |
| RC-04 | 继承约束已加载 | 上下文包含"继承约束链" | [TODOLIST-ERROR] |
| RC-05 | 上下文大小合理 | 预估 ≤15K tokens | 压缩父级历史 |

**全部 PASS → 开始执行**
**任一 FAIL → [TODOLIST-ERROR] 规则重载失败，Agent 已停止**

---

## 1. 递归分解结果

| ID | 名称 | 层级 | 父节点 | 预估行数 | 状态 | 执行顺序 | 依赖 | 全局 REQ | E-REQ |
|----|------|------|--------|----------|------|----------|------|----------|-------|
| 0 | 顶层模块 | L0 | - | {N} | completed | 0 | - | REQ-001~REQ-033 | - |
| 1 | {子模块A} | L1 | 顶层 | {N} | pending | 1 | - | REQ-XXX | E-001~E-005 |
| 1.1 | {子模块A1} | L2 | A | {N} | pending | 1.1 | - | REQ-XXX | E-001~E-003 |
| 1.1.1 | {子模块A1a} | L3 | A1 | {N} | pending | 1.1.1 | - | REQ-XXX | E-001~E-002 |

**状态说明**：pending -> in_progress -> completed -> skipped
**起始阶段**：所有子模块从 stageB+ 开始（非 D0）
**递归终止**：所有叶子节点预估行数 <3000

---

## 2. 父模块上下文摘要

> 本节提供父模块的关键信息，供子模块执行时参考。无需回溯父模块文档。

| 字段 | 内容 |
|------|------|
| 父模块名称 | {parent_name} |
| 父模块功能 | {一句话描述父模块核心功能} |
| 父模块关键约束 | {列出影响子模块的约束，如时钟域/功耗域/接口协议} |
| 父模块与兄弟模块关系 | {列出兄弟模块名称和接口方向} |
| 父模块 REQ 汇总 | {列出与本子模块相关的父模块 REQ 编号和描述} |

---

## 3. 本模块接口契约

> 本节定义本模块与父模块/兄弟模块的接口，供 stageD 接口设计时参考。

### 3.1 与父模块接口

| 端口名 | 方向 | 位宽 | 协议 | 父模块端口 | 说明 |
|--------|------|------|------|-----------|------|
| {port} | I/O | {W} | {协议} | {parent_port} | {说明} |

### 3.2 与兄弟模块接口

| 兄弟模块 | 端口名 | 方向 | 位宽 | 协议 | 说明 |
|----------|--------|------|------|------|------|
| {sibling} | {port} | I/O | {W} | {协议} | {说明} |

---

## 4. 继承约束链

> 本节列出从 L0 逐级传递到本模块的约束。每条标注来源层级。

| REQ 编号 | 来源层级 | 约束描述 | 对本模块的影响 |
|----------|----------|----------|---------------|
| REQ-001 | L0 | {约束描述} | {对本模块的影响} |
| REQ-006 | L0 | {约束描述} | {对本模块的影响} |
| E-001 | L1 | {约束描述} | {对本模块的影响} |

---

## 5. 子模块 flow 定义

> **铁律：子模块必须严格按以下 flow 顺序执行。缺失则 [TODOLIST-ERROR] 停止。**

| Stage | 名称 | 必须执行 | 最小 Q&A 数 | 必须产出文件 | 详细要求 |
|-------|------|----------|------------|-------------|----------|
| B+ | 头脑风暴 Feature Discovery | 是 | 5 轮（5 维度） | flow/stageB_plus.md | 参考 Wiki，5 维度探索，用户确认后才进入下一阶段 |
| C0 | 矛盾检测 | 是 | 17 项检测 | flow/stageC0.md | 基础 17 项 + 条件检测 + 覆盖率热力图 |
| C | 需求确认汇总 | 是 | 1 轮确认 | outputs/{name}_requirement_summary_v1.0.md | E-REQ 汇总表 + 规格自检 5 项 |
| D | 方案细化 | 是 | D0=4, 其他=3 | outputs/{name}_solution_v1.0.md + ADR | D0 估算行数 + D1~D14 逐阶段 |
| E | 递归分解 | 条件 | - | {name}_todolist.md | D0 >3000 行时触发，生成下级 todolist |

### 5.1 stageB+ 详细要求

- 参考 Wiki 知识库检索相关协议/CBB/行业实践
- 按 5 个维度探索：功能扩展/性能优化/兼容性/可测试性/可维护性
- 每轮后用户确认才进入下一阶段
- 不确认则继续分解直到无新 feature（最多 5 轮）
- 追加 E-REQ 从 E-{N+1} 起连续编号

### 5.2 stageD 详细要求

- D0：估算 RTL 行数（功能+接口+控制+存储），>3000 行触发 E 阶段
- D1~D14：每个子阶段前置分析 + 头脑风暴 + 输出物
- 跳过的子阶段：ADR 标注原因+影响+替代方案
- D14 完成后：devils-advocate gentle + balanced

---

## 6. 交付文件内容模板

> 每个 outputs/ 文件必须按以下模板生成，禁止使用占位符。

### 6.1 {name}_pr_v1.0.md（PR 沟通记录）

```
# {模块名} PR 沟通记录
> 版本：v1.0 | 日期：YYYY-MM-DD

## stageB+ 头脑风暴
### Q1: {问题}
A: {回答}

## stageC0 矛盾检测
{17 项检测结果}

## stageC 需求确认
{需求汇总表 + 规格自检}

## stageD 方案细化
### D0: RTL 行数估算
{估算过程和结论}
### D1~D14: {每阶段 Q&A}

## stageE 递归分解（如有）
{递归分解过程}
```

### 6.2 {name}_requirement_summary_v1.0.md（需求汇总表）

```
# {模块名} 需求汇总表
> schema_version: 1.0

| E-REQ | 约束项 | 确认值 | 优先级 | 备注 |
|-------|--------|--------|--------|------|
| E-001 | {项} | {值} | Must/Should/Could | {说明} |

## 规格自检
| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 占位符扫描 | PASS/FAIL |
| 2 | 内部一致性 | PASS/FAIL |
| 3 | 范围检查 | PASS/FAIL |
| 4 | 模糊性检查 | PASS/FAIL |
| 5 | REQ 覆盖度 | PASS/FAIL |
```

### 6.3 {name}_solution_v1.0.md（方案文档）

```
# {模块名} 方案文档

## S3 架构概述
## S4 接口定义
## S5 数据通路与控制逻辑
## S6 关键时序分析
## S7 寄存器定义
## S8 PPA 预估
## S9 时钟与复位
## S10 低功耗设计
## S11 DFT 设计
## S12 可靠性设计
## S13 约束与假设
## S14 顶层集成
```

### 6.4 {name}_ADR_v1.0.md（ADR 文档）

```
# {模块名} 架构决策记录

## ADR-001: {决策标题}
- 背景：{问题和约束}
- 决策：{明确选择}
- 方案对比：A({优劣}) vs B({优劣})
- 后果：正面/负面/风险
- 关联 REQ：REQ-XXX

## 方案细化记录
### D0: {名称}
{分析结论 + Q&A 记录 + 方案决策}
```

### 6.5 {name}_trace_graph.yaml

```yaml
metadata:
  module: {name}
  version: v1.0
  last_updated: YYYY-MM-DD

nodes:
  - id: E-001
    layer: L1
    type: requirement
    title: "{需求标题}"
    ref: "outputs/{name}_requirement_summary_v1.0.md"
    upstream: []
    downstream: []
```

---

## 7. Wiki 知识库参考

> 头脑风暴和方案设计时参考以下 Wiki 知识。

| 知识类型 | Wiki 页面 | 用途 |
|----------|----------|------|
| 协议规范 | {wiki_page} | {用途} |
| CBB 选型 | {wiki_page} | {用途} |
| 设计模式 | {wiki_page} | {用途} |
| 行业实践 | {wiki_page} | {用途} |

---

## 8. 质量门控检查清单

> 每项必须用具体命令验证，禁止凭印象给分。

| # | 检查项 | 验证命令 | PASS 条件 | FAIL 处理 |
|---|--------|----------|-----------|-----------|
| QG-01 | outputs/ 文件数 | find {dir}/outputs/ -type f | >= 5 | 阻断 |
| QG-02 | flow/ 文件数 | find {dir}/flow/ -type f | >= 4 | 阻断 |
| QG-03 | flow/stageB_plus.md 存在 | ls {dir}/flow/stageB_plus.md | 存在 | 阻断 |
| QG-04 | E-REQ 编号连续 | grep E-REQ {dir}/outputs/*_req* | 无跳号 | 阻断 |
| QG-05 | 方案文档章节完整 | grep "## S" {dir}/outputs/*_sol* | >= 10 | 阻断 |
| QG-06 | 无占位符 | grep "TODO" {dir}/outputs/*.md | = 0 | 阻断 |
| QG-07 | 文件命名规范 | ls {dir}/outputs/ | 全部 {name}_ 前缀 | 阻断 |
| QG-08 | 文件大小 > 1KB | wc -c {dir}/outputs/*.md | 全部 > 1024 | 阻断 |

**全部 PASS -> 更新状态为 completed**
**任一 FAIL -> 停止执行，报告 [TODOLIST-ERROR]**

---

## 9. 执行进度跟踪

| 层级 | 模块 | 总数 | completed | in_progress | pending | 进度 |
|------|------|------|-----------|-------------|---------|------|
| L0 | 顶层 | 1 | {N} | - | - | {N}% |
| L1 | 子模块 | {N} | {N} | {N} | {N} | {N}% |
| L2 | 孙模块 | {N} | {N} | {N} | {N} | {N}% |
| L3 | 曾孙模块 | {N} | {N} | {N} | {N} | {N}% |
| **全局** | **所有** | **{N}** | **{N}** | **{N}** | **{N}** | **{N}%** |

---

## 10. todolist 完整性自检

> Agent 执行前必须检查本 todolist 是否完整。缺失则 [TODOLIST-ERROR] 停止。

| # | 必填项 | 检查方法 | 缺失处理 |
|---|--------|----------|----------|
| T-01 | S1 递归分解结果表格 | 表格存在且有数据 | [TODOLIST-ERROR] |
| T-02 | S2 父模块上下文摘要 | 非空 | [TODOLIST-ERROR] |
| T-03 | S3 接口契约 | 至少 1 个端口定义 | [TODOLIST-ERROR] |
| T-04 | S4 继承约束链 | 至少 1 条约束 | [TODOLIST-ERROR] |
| T-05 | S5 flow 定义 | 每个子模块有 flow 列 | [TODOLIST-ERROR] |
| T-06 | S6 输出模板 | 每个文件类型有模板 | [TODOLIST-ERROR] |
| T-07 | S8 质量门控 | 至少 5 项检查 | [TODOLIST-ERROR] |

**全部 PASS -> 开始执行**
**任一 FAIL -> [TODOLIST-ERROR] {级}.{模块} todolist 缺失 {项}，Agent 已停止**
