# Blackbox Module List

> Modules listed here are BLACKBOX — all lint violations from these modules are auto-waived.
> Add module names (one per line) below. Lines starting with `#` are comments.

## Format

```
<module_name>  [<reason>]
```

- `module_name`: exact module name as it appears in lint log file paths or violation messages
- `reason`: optional, why this module is blackbox (e.g., "third-party IP", "pre-verified", "not owned by us")

## Blackbox Modules

<!-- Add your blackbox modules below this line -->
