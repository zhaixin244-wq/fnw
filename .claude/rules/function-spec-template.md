---
name: Chip Function Specification Template
description: 芯片模块功能规格（FS）文档模板。
---

# {模块名} 功能规格书

> `{...}` 为占位符，编写时替换为实际内容。`[可选]` 根据模块特性决定是否包含。所有 PPA 必须量化。每个需求分配唯一 REQ-XXX。

---

## 1. 文档信息

| 字段 | 内容 |
|------|------|
| **模块名称** | `{module_name}` |
| **版本** | `v1.0` |
| **文档编号** | `{project_code}-FS-{module_name}-v1.0` |
| **作者/日期/审核/批准** | `{author}` / `YYYY-MM-DD` / `{reviewer}` / `{approver}` |

---

## 2. 修订历史

| 版本 | 日期 | 作者 | 变更描述 |
|------|------|------|----------|
| v1.0 | YYYY-MM-DD | {author} | 初始版本 |

---

## 3. 概述

### 3.1 模块定位
> 模块在 SoC/芯片中的位置，与上下游模块的关系。

**关键特性列表**：

| 编号 | 特性 | 说明 |
|------|------|------|
| F-001 | {特性名称} | {简述} |

### 3.2 顶层框图与数据通路

使用 `chip-png-d2-gen` 生成 D2 架构框图（`{module}_arch.d2`），通过 `d2 --layout dagre` 编译为 PNG。标注信号流向和数据宽度。

```markdown
![{模块名} 顶层框图与数据通路]({module}_arch.png)
```

---

## 4. 功能描述

### 4.1 功能概述
> 3-5 句话描述核心功能。

> **追溯规则**：每条功能描述必须追溯到具体 REQ 编号，格式为 `**REQ-XXX**：{描述}`。未追溯的功能描述视为未分配需求，需在 RTM（§14）中标注原因。REQ 编号范围从上游 REQ 汇总表（阶段 C 输出）获取，典型为 REQ-001~REQ-021。

**REQ-001**：{需求描述}

### 4.2 工作模式

每个模式包含：触发条件、行为描述、退出条件、异常处理。附状态转移图（使用 `chip-png-d2-gen` 生成 D2 状态机图，`d2 --layout dagre` 编译为 PNG）。

### 4.3 数据流描述

| 阶段 | 输入格式 | 处理操作 | 输出格式 | 延迟(cycles) |
|------|----------|----------|----------|-------------|
| 阶段1 | {格式} | {操作} | {格式} | {N} |

### 4.4 控制流描述

**仲裁策略**（如有多源竞争）：

| 仲裁器 | 仲裁对象 | 策略 | 优先级 | RR 更新时机 |
|--------|----------|------|--------|------------|
| `{arbiter}` | {写端口/读端口} | {Fixed/RR/WRR} | {优先级表} | {包边界/事务边界} |

**流控机制**：

| 流控类型 | 接口方向 | 机制 | 背压传播路径 |
|----------|----------|------|-------------|
| 背压 | {输入/输出} | ready 反压 | {下游 → 本级 → 上游} |
| 信用 | {输入/输出} | {信用计数} | {描述} |

**超时处理**：

| 超时场景 | 超时阈值 | 超时行为 | 恢复方式 | 相关信号 |
|----------|----------|----------|----------|----------|
| {握手超时} | {N} cycles | {报错/重试/丢弃} | {自动/软件} | `{signal}` |

---

## 5. 子模块详细设计

> 本章逐个描述模块内各子模块的功能、接口与接口时序，为 RTL 实现提供直接依据。每个子模块独立一节，包含功能描述、端口列表、接口时序和背压/流控策略。

### 5.1 {子模块名1}

#### 5.1.1 功能描述
> 2-3 句话描述该子模块的核心功能和在上级模块中的角色。

**职责范围**：
- {职责1}
- {职责2}

**工作模式**：{简述工作模式，如"始终运行"/"由 {enable} 信号控制"/"按需触发"，详细状态机见微架构文档}

#### 5.1.2 端口列表

> 类型列选择规则：输入端口为 `wire`，时序输出为 `reg`，组合输出为 `wire`（遵循编码规范 §3）。

| # | 信号名 | 方向 | 位宽 | 类型 | 时钟域 | 复位值 | 功能描述 |
|---|--------|------|------|------|--------|--------|----------|
| 1 | `clk` | I | 1 | wire | - | - | 主时钟 |
| 2 | `rst_n` | I | 1 | wire | - | - | 低有效异步复位 |
| 3 | `{signal}` | I/O | {W} | wire/reg | clk | {0/1} | {描述} |

**与顶层接口映射**：

| 子模块端口 | 顶层接口 | 信号名 | 映射说明 |
|------------|----------|--------|----------|
| `{port}` | §6.{N} | `{top_signal}` | 直连/位宽适配/组合逻辑 |

#### 5.1.3 接口协议与时序

**协议类型**：{Valid-Ready / 请求-应答 / 寄存器映射 / 自定义}

**事务流程**：使用 `chip-png-wavedrom-gen` 生成 Wavedrom 时序图（`wd_{描述}.json`），编译为 PNG。

```markdown
![{事务名称} 时序](wd_{描述}.png)
```

**关键时序参数**：

| 参数 | 值 | 单位 | 说明 |
|------|----|------|------|
| 输入 setup | ≤ {N} | ns | 相对 clk 上升沿 |
| 输出 delay | ≤ {N} | ns | clk 到输出有效 |
| 握手响应 | ≤ {N} | cycles | valid 到 ready 最大延迟 |
| 超时阈值 | {N} | cycles | 握手超时判定 [可选] |

#### 5.1.4 流控与背压

| 流控类型 | 接口方向 | 机制 | 背压路径描述 |
|----------|----------|------|-------------|
| 背压 | {输入/输出} | ready 反压 | {下游 ready 拉低 → 本级 ready 拉低 → 上游停止} |
| 信用 | {输入/输出} | {信用计数} | {描述} [可选] |

**背压传播规则**：{描述 valid/ready 依赖关系，确保无组合环路}

**超时/异常处理**：

| 异常场景 | 触发条件 | 子模块行为 | 恢复方式 | 相关信号 |
|----------|----------|----------|----------|----------|
| {异常} | {条件} | {行为} | {恢复} | `{signal}` |

---

### 5.2 {子模块名2}

#### 5.2.1 功能描述
> （同 5.1.1 格式）

#### 5.2.2 端口列表
> （同 5.1.2 格式）

#### 5.2.3 接口协议与时序
> （同 5.1.3 格式）

#### 5.2.4 流控与背压
> （同 5.1.4 格式）

---

> **编写指南**：

---

### 5.N 子模块间接口 [可选]

> 如子模块间存在内部接口（非顶层可见），在此描述。外部可见接口在第6章定义。

#### 5.N.1 内部接口列表

| 接口名称 | 源子模块 | 目标子模块 | 协议类型 | 位宽 | 说明 |
|----------|----------|------------|----------|------|------|
| `{if_name}` | §5.{src} | §5.{dst} | {Valid-Ready/自定义} | {W} | {说明} |

#### 5.N.2 内部接口信号

| 信号名 | 方向 | 位宽 | 源 | 目标 | 功能描述 |
|--------|------|------|------|--------|----------|
| `{signal}` | I/O | {W} | §5.{src} | §5.{dst} | {描述} |

> **约束**：内部接口信号不暴露到顶层端口，仅用于子模块间互联。若需在第6章暴露，必须在顶层接口映射表中声明。

> **编写指南**：
> 1. 每个子模块独立一节（5.1, 5.2, ...），编号连续
> 2. 端口列表必须包含所有信号，位宽用参数表示（如 `DATA_WIDTH`），禁止硬编码
> 3. 接口时序使用 `chip-png-wavedrom-gen` 生成 Wavedrom 时序图（`wd_*.json` → PNG），使用 `chip-png-interface-gen` 生成接口端口图
> 4. 顶层接口映射表确保子模块端口与第6章顶层信号一一对应
> 5. 流控策略必须明确 valid/ready 依赖关系，防止组合环路
> 6. 异常处理覆盖所有可能的错误场景
> 7. 子模块内部详细实现（状态机、FIFO、流水线）在微架构文档中定义，FS 中仅描述外部可见行为

---

## 6. 顶层接口定义

### 6.1 接口列表

| 接口名称 | 协议类型 | 方向 | 位宽 | 时钟域 | 说明 |
|----------|----------|------|------|--------|------|
| `{if_name}` | {协议} | {方向} | {位宽} | clk | {说明} |

### 6.2 信号详细列表

| 信号名 | 方向 | 位宽 | 时钟域 | 复位值 | 功能描述 |
|--------|------|------|--------|--------|----------|
| `{signal}` | I/O | {W} | clk | {0/1} | {描述} |

### 6.3 接口时序要求
> 关键时序参数表。使用 `chip-png-wavedrom-gen` 生成 Wavedrom 时序图，使用 `chip-png-interface-gen` 生成接口端口图。

### 6.4 异常行为处理

| 异常场景 | 触发条件 | 模块行为 | 恢复方式 | 相关信号 |
|----------|----------|----------|----------|----------|
| {异常} | {条件} | {行为} | {恢复} | `{signal}` |

---

## 7. 寄存器定义

### 7.1 地址映射表

| 偏移地址 | 名称 | 访问类型 | 复位值 | 描述 |
|----------|------|----------|--------|------|
| `0x00` | `CTRL` | RW | `0x0000_0000` | 控制寄存器 |
| `0x04` | `STATUS` | RO | `0x0000_0000` | 状态寄存器 |

访问类型：`RW`=读写，`RO`=只读，`W1C`=写1清零，`W0C`=写0清零，`W1S`=写1置位，`WO`=只写，`RSVD`=保留

### 7.2 寄存器位域定义

每个寄存器按以下格式定义：

**`{寄存器名}`（偏移 `0x{XX}`）**：{功能描述}

| Bit | 名称 | 访问 | 复位值 | 描述 |
|-----|------|------|--------|------|
| [31:16] | `{field}` | RW | `0x0000` | {描述} |
| [15:8] | `{field}` | RO | `0x00` | {描述} |
| [7:4] | `{field}` | W1C | `0x0` | {描述} |
| [3:0] | `{field}` | RSVD | `0x0` | 保留 |

访问类型：`RW`=读写，`RO`=只读，`W1C`=写1清零，`W0C`=写0清零，`W1S`=写1置位，`WO`=只写，`RSVD`=保留

---

## 8. PPA 规格

> 所有指标必须指定操作条件。无法量化的标注"目标值，待综合验证"。

### 8.1 性能指标

| 指标 | 数值 | 单位 | 约束类型 | 条件 |
|------|------|------|----------|------|
| 处理延迟 | ≤ {N} | cycles | Target | 输入到输出 |
| 吞吐 | ≥ {N} | ops/cycle | Target | 连续输入 |
| 带宽 | ≥ {N} | GB/s 或 bytes/cycle | Target | 连续数据流 |
| 最大频率 | ≥ {N} | MHz | Target | Fmax |

### 8.2 功耗指标（PVT Corner：`{TT/SS/FF}` / `{V}V` / `{T}°C`）

| 指标 | 数值 | 单位 | 约束类型 |
|------|------|------|----------|
| 动态功耗 | < {N} | mW | Budget |
| 静态功耗 | < {N} | mW | Budget |

### 8.3 面积指标

| 指标 | 数值 | 单位 | 约束类型 |
|------|------|------|----------|
| 逻辑面积 | ~{N} | kGates | Estimated |
| SRAM | {N} | Kbit | Estimated |

### 8.4 子模块 PPA 预算分配（如适用）

| 子模块 | 延迟 | 面积 | 功耗 |
|--------|------|------|------|
| {sub} | {N} | {N} | {N} |
| **合计** | **{N}** | **{N}** | **{N}** |

---

## 9. 时钟与复位

| 时钟域 | 频率 | 来源 | 说明 |
|--------|------|------|------|
| `clk` | {N} MHz | PLL | 主时钟 |

复位策略：异步复位同步释放，低有效 `rst_n`，2 级同步器。

---

## 10. 低功耗设计 [可选]

如不涉及特殊低功耗设计，标注"使用全局 Clock Gating 方案"并跳过本章。否则按以下模板定义：

### 10.1 功耗域划分

| 功耗域 | 电压 | 包含模块 | 关断策略 | 状态保持 |
|--------|------|----------|----------|----------|
| `{domain}` | {V} | {模块列表} | {Always-On/Sleep/Deep-Sleep/Off} | {Retention FF/软件保存/无} |

### 10.2 隔离与电平转换

| 信号方向 | 源域 | 目标域 | 隔离类型 | 钳位值 |
|----------|------|--------|----------|--------|
| {输出} | {domain_A} | {domain_B} | Isolation Cell | {协议默认值} |

| 跨压方向 | 源电压 | 目标电压 | Level Shifter 类型 |
|----------|--------|----------|-------------------|
| 低→高 | {V_low} | {V_high} | LHSx |
| 高→低 | {V_high} | {V_low} | HLSx |

### 10.3 功耗状态机

| 当前状态 | 目标状态 | 触发条件 | 切换延迟 | 保持寄存器 |
|----------|----------|----------|----------|-----------|
| Active | Sleep | {条件} | {N} cycles | {列表} |
| Sleep | Active | {条件} | {N} cycles | - |

### 10.4 功耗预算分配

| 功耗域 | 动态功耗 | 静态功耗 | 合计 |
|--------|----------|----------|------|
| {domain} | {N} mW | {N} mW | {N} mW |
| **合计** | **{N} mW** | **{N} mW** | **{N} mW** |

---

## 11. DFT 设计 [可选]

如不涉及特殊 DFT，标注"遵循项目通用 DFT 规则（扫描链、标准 ICG）"并跳过本章。否则按以下模板定义：

### 11.1 扫描链约束

| 约束项 | 配置 | 说明 |
|--------|------|------|
| 扫描链数量 | {N} 条 | {说明} |
| 分域策略 | {全局/按功耗域} | {说明} |
| 扫描使能信号 | `{signal}` | {说明} |

### 11.2 ICG 配置

| 时钟 | ICG Cell | 使能信号 | Scan Enable |
|------|----------|----------|-------------|
| `{clk}` | `{cell_name}` | `{en}` | `{scan_en}` |

### 11.3 MBIST/LBIST [可选]

| 测试类型 | 覆盖范围 | 接口 | 说明 |
|----------|----------|------|------|
| MBIST | {SRAM 列表} | {接口} | {说明} |
| LBIST | {逻辑范围} | {接口} | {说明} |

---

## 12. 可靠性设计 [可选]

如不涉及特殊可靠性设计，标注"遵循项目通用可靠性规则"并跳过本章。否则按以下模板定义：

### 12.1 ECC/Parity 方案

| 存储/信号 | 保护类型 | 编码 | 检错/纠错能力 | 面积开销 |
|-----------|----------|------|--------------|----------|
| `{name}` | ECC/Parity | {SECDED/ChipKill/奇偶} | {1bit纠错+2bit检错/1bit检错} | {N}% |

### 12.2 SEU 防护

| 防护对象 | 策略 | 刷新周期 | 说明 |
|----------|------|----------|------|
| {SRAM/寄存器} | {TMR/刷新/ECC} | {N} ms | {说明} |

### 12.3 可靠性预算

| 指标 | 目标值 | 说明 |
|------|--------|------|
| FIT 率 | ≤ {N} | {说明} |
| MTBF | ≥ {N} hours | {说明} |
| 老化裕量 | ≥ {N}% | {说明} |

---

## 13. 约束与假设

### 设计约束
| 约束项 | 约束值 | 说明 |
|--------|--------|------|
| 工艺节点 | {nm} | |
| 频率 | {N} MHz | |
| 总线协议/位宽 | {AXI4/AHB} / {N} bit | |

### 假设条件与依赖项
| 编号 | 描述 | 验证方式/版本 |
|------|------|--------------|
| A-001 | {假设} | {验证} |

---

## 14. 需求追溯矩阵（RTM）

| 需求 ID | 优先级 | 需求描述 | FS 章节 | 接口/信号 | PPA 指标 | 验证策略 | 状态 |
|---------|--------|----------|---------|-----------|----------|----------|------|
| REQ-001 | Must | {描述} | §4.1 | - | - | {策略} | Allocated |

**优先级**：`Must`=方案必须满足 / `Should`=方案应尽量满足 / `Could`=方案可选优化

状态：`Allocated` → `Designed` → `Verified` → `Waived`

**追溯覆盖率**：

| 统计项 | 数值 |
|--------|------|
| REQ 总数 | {N} |
| 已覆盖 | {N} ({N/N×100}%) |
| 未覆盖 | {N} ({原因}) |
| 未覆盖项 | {REQ-XXX: 原因} |

---

## 15. 附录

### 参考文档

| 编号 | 文档名 | 说明 |
|------|--------|------|
| REF-001 | `{module_name}_requirement_summary_v{X}.md` | 需求汇总表 |
| REF-002 | `{module_name}_solution_v{X}.md` | 架构方案文档 |
| REF-003 | `{module_name}_ADR_v{X}.md` | 架构决策记录 |
| REF-004 | `{协议规范文档}` | 接口协议规范 |
| REF-005 | `{CBB/IP 文档}` | 使用的 CBB/IP 参考手册 |

### 缩略语

| 缩写 | 全称 |
|------|------|
| AXI | Advanced eXtensible Interface |
| CDC | Clock Domain Crossing |
| DFT | Design For Testability |
| PPA | Performance, Power, Area |
| RTM | Requirements Traceability Matrix |
| SVA | SystemVerilog Assertion |
