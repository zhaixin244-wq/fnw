# bitstream_val_mgr PR 沟通记录

> 版本：v1.0
> 日期：2026-06-04
> 阶段：stageB phase2（执行中）
> 当前进度：stageB phase2（执行中）
> 已完成：（无）
> 父模块：fpga_partial_reconfig
> 子模块 ID：2（L1）
> 上游依赖：bitstream_rx_engine（子模块 1）

---

## 父模块上下文摘要

**功能**：FPGA 部分重配置控制器，通过 PCIe DMA 或本地 Flash 接收比特流，在运行时对 FPGA 部分区域进行动态重配置，支持多租户隔离和比特流完整性校验。

**关键约束**：16nm / 500MHz / 逻辑 < 200kGates / 动态功耗 < 180mW

**上游模块**：bitstream_rx_engine — 接收原始比特流数据，解析头部信息，写入 SRAM 缓存

**下游模块**：icap_write_engine — 接收校验通过的比特流，写入 FPGA ICAP 接口

---

## 继承 REQ（同步自父模块）

| 全局 REQ | 约束项 | 确认值 | 优先级 | 适用性 |
|----------|--------|--------|--------|--------|
| REQ-003 | 数据流特征 | 比特流 8~32MB / ICAP 32-bit 粒度 | Should | 适用——本模块处理数据流 |
| REQ-004 | 延迟与吞吐 | 整体重配延迟 < 200ms | Should | 适用——校验+解压延迟计入总延迟 |
| REQ-009 | 可靠性 | CRC-32 校验 + 回滚机制 | Must | 适用——本模块核心功能 |
| REQ-030 | 比特流压缩/解压 | 先支持 LZ4，Zlib 可选 | Should | 适用——本模块实现解压 |
| REQ-031 | 传输-校验-写入流水线 | 三阶段重叠，DMA 传输与 CRC 校验并行 | Should | 适用——本模块实现校验段 |
| REQ-032 | 比特流版本管理 | 记录版本号，支持回退 | Should | 适用——本模块管理版本 |
| REQ-033 | 错误注入接口 | APB 可注入 CRC 错误/超时 | Should | 适用——本模块实现 CRC 错误注入 |

---

## [PHASE-START] stageB phase2

### 头脑风暴 Feature Discovery

**触发条件**：子模块从 stageB phase2 开始
**执行方式**：按 5 个维度探索（功能扩展/性能优化/兼容性/可测试性/可维护性）

---

#### 维度 1：功能扩展

**Q1**：bitstream_val_mgr 在校验和管理方面，是否有父模块 REQ 未覆盖的额外功能需求？

**A1**（基于功能分析）：

| 潜在 Feature | 说明 | 建议优先级 | 与父模块 REQ 关系 |
|--------------|------|-----------|-------------------|
| 多算法 CRC 支持 | 支持 CRC-32/CRC-16/自定义多项式 | Could | 扩展 REQ-009 |
| 比特流格式校验 | 校验比特流格式合法性（魔数/版本/长度） | Must | 补充 REQ-009 |
| 解压流水线 | CRC 校验与 LZ4 解压重叠执行 | Should | 优化 REQ-030/031 |
| 回滚版本选择 | 回滚时可选择目标版本，而非仅回滚到上一版本 | Should | 扩展 REQ-032 |

---

#### 维度 2：性能优化

**Q2**：在校验和解压性能方面，是否有额外优化需求？

**A2**（基于功能分析）：

| 潜在 Feature | 说明 | 建议优先级 | 与父模块 REQ 关系 |
|--------------|------|-----------|-------------------|
| CRC 流式计算 | CRC 边接收边计算，不等待完整比特流 | Should | 优化 REQ-009/031 |
| LZ4 并行解压 | 多个解压引擎并行处理不同数据块 | Could | 扩展 REQ-030 |
| 校验结果预取 | CRC 校验完成前预取下一比特流段 | Should | 优化 REQ-031 |

---

#### 维度 3：兼容性

**Q3**：是否需要兼容不同版本的比特流格式或压缩算法？

**A3**（基于功能分析）：

| 潜在 Feature | 说明 | 建议优先级 | 与父模块 REQ 关系 |
|--------------|------|-----------|-------------------|
| 比特流格式版本协商 | 通过头部版本号自动选择解析策略 | Should | 扩展 REQ-032 |
| 压缩算法自动检测 | 根据比特流头部标识自动选择 LZ4/Zlib | Should | 扩展 REQ-030 |

---

#### 维度 4：可测试性

**Q4**：在校验管理器的验证方面，有哪些额外需求？

**A4**（基于功能分析）：

| 潜在 Feature | 说明 | 建议优先级 | 与父模块 REQ 关系 |
|--------------|------|-----------|-------------------|
| CRC 错误注入可控 | 精确控制 CRC 错误注入位置和类型 | Should | 扩展 REQ-033 |
| 解压结果校验 | 解压后二次校验数据完整性 | Should | 补充 REQ-009 |
| 校验统计寄存器 | 校验通过/失败/回滚次数统计 | Should | 调试支持 |

---

#### 维度 5：可维护性

**Q5**：在校验管理器的运维和调试方面，有哪些需求？

**A5**（基于功能分析）：

| 潜在 Feature | 说明 | 建议优先级 | 与父模块 REQ 关系 |
|--------------|------|-----------|-------------------|
| 校验耗时统计 | 记录每次校验和解压的耗时 | Should | 扩展 REQ-035 |
| 错误详情日志 | 记录最近 N 次校验错误的详细信息（位置/类型） | Should | 调试支持 |
| 回滚历史记录 | 记录回滚操作的历史（源版本/目标版本/时间） | Should | 扩展 REQ-032 |

---

### 头脑风暴追加 REQ 汇总

基于 5 个维度探索，以下 Feature 建议追加为子模块本地 REQ：

| 本地 REQ | Feature | 维度 | 优先级 | 确认值 |
|----------|---------|------|--------|--------|
| LREQ-001 | 比特流格式校验 | 功能扩展 | Must | 校验比特流格式合法性（魔数/版本/长度字段） |
| LREQ-002 | CRC 流式计算 | 性能优化 | Should | CRC 边接收边计算，不等待完整比特流 |
| LREQ-003 | 解压流水线 | 性能优化 | Should | CRC 校验与 LZ4 解压重叠执行 |
| LREQ-004 | 回滚版本选择 | 功能扩展 | Should | 回滚时可选择目标版本，而非仅回滚到上一版本 |
| LREQ-005 | 压缩算法自动检测 | 兼容性 | Should | 根据比特流头部标识自动选择 LZ4/Zlib |
| LREQ-006 | 解压结果校验 | 可测试性 | Should | 解压后二次校验数据完整性 |
| LREQ-007 | 校验统计寄存器 | 可测试性 | Should | 校验通过/失败/回滚次数统计 |
| LREQ-008 | 错误详情日志 | 可维护性 | Should | 记录最近 N 次校验错误的详细信息 |

### 矛盾检查

| 检查对 | 结果 | 说明 |
|--------|------|------|
| LREQ-002（流式 CRC）vs REQ-009（CRC-32） | 无矛盾 | 流式计算是 CRC 实现优化 |
| LREQ-003（解压流水线）vs REQ-004（延迟 <200ms） | 无矛盾 | 流水线减少总延迟，有利于满足约束 |
| LREQ-005（自动检测）vs REQ-030（LZ4 先行） | 无矛盾 | 自动检测是 REQ-030 的扩展 |

**结论**：追加 8 个本地 REQ，无矛盾。

### 用户确认

用户确认 8 个本地 REQ 均可接受。补充建议：LREQ-005 压缩算法自动检测，建议在比特流头部增加算法标识字段（0x00=未压缩，0x01=LZ4）。

## [PHASE-END] stageB phase2

---

## [PHASE-START] stageC phase1

### 矛盾检测执行

**检测范围**：
- 基础检测：17 条（针对本子模块继承 REQ + 本地 REQ 适用项）
- 动态检测：3 条（DYN-01/02/03，针对本地 REQ vs 继承 REQ）
- 条件检测：不适用（REQ-022~025 均为 skipped）

**检测结果**：

| # | 规则 ID | 规则名称 | 涉及 REQ | 结果 | 说明 |
|---|---------|----------|----------|------|------|
| 1 | FREQ-01 | 频率 vs 工艺 | REQ-001(继承) | PASS | 16nm/500MHz，校验+解压逻辑远低于 Fmax |
| 2 | FREQ-02 | 延迟 vs 频率 | REQ-004(继承) | PASS | 200ms 系统延迟，校验+解压延迟裕量充足 |
| 3 | AREA-01 | 面积 vs 功能复杂度 | REQ-005(继承) | PASS | 950 行预估，逻辑面积 ~45kGates，在预算内 |
| 4 | AREA-02 | 面积 vs 并行度 | REQ-003(继承) | PASS | 单引擎处理，无多路并行 |
| 5 | AREA-03 | 可靠性 vs 面积 | REQ-009(继承) | PASS | CRC-32 面积开销 < 5kGates |
| 6 | POWER-01 | 功耗 vs 工艺/频率 | REQ-001/005(继承) | PASS | 校验+解压逻辑功耗 < 25mW |
| 7 | POWER-02 | 低功耗 vs DFT | REQ-007/008(继承) | PASS | 全局 Clock Gating + 标准扫描链 |
| 8 | IO-01 | 接口位宽 vs 数据粒度 | REQ-002/003(继承) | PASS | 32-bit 数据通路与 ICAP 粒度匹配 |
| 9 | IO-02 | 多模块共享资源竞争 | REQ-012(继承) | PASS | SRAM 1R1W 双端口，读写可并行 |
| 10 | IO-03 | 安全隔离 vs 地址碎片化 | REQ-018(继承) | N/A | 本子模块不涉及隔离 |
| 11 | PVT-01 | PVT vs 功耗预算 | REQ-013(继承) | PASS | 校验+解压逻辑功耗裕量充足 |
| 12 | MEM-01 | 存储容量 vs 面积 | REQ-012(继承) | PASS | 无额外存储需求 |
| 13 | CDC-01 | CDC vs 时序裕量 | REQ-011(继承) | N/A | 本子模块在主时钟域内，无 CDC |
| 14 | PERF-01 | 延迟 vs 吞吐 | REQ-004(继承) | PASS | 流水线设计减少总延迟 |
| 15 | DYN-01 | 功能重叠 | LREQ vs 继承 REQ | PASS | LREQ-001(格式校验)与 REQ-032(版本管理)功能不同 |
| 16 | DYN-02 | PPA 交叉矛盾 | LREQ vs 继承 REQ | PASS | 本地 REQ 无额外 PPA 约束 |
| 17 | DYN-03 | 接口冲突 | LREQ vs REQ-002 | PASS | 本地 REQ 均使用内部接口 |

---

### 矛盾检测结果汇总

| 类别 | 数量 |
|------|------|
| PASS | 15 |
| WARN | 0 |
| N/A | 2 |
| **矛盾** | **0 个** |
| **警告** | **0 个** |

---

## [PHASE-END] stageC phase1

---

## [PHASE-START] stageC phase2

### 需求确认汇总表

> schema_version: 1.0
> module_name: bitstream_val_mgr
> parent_module: fpga_partial_reconfig
> date: 2026-06-04
> total_reqs: 15（7 继承 + 8 本地）
> priority_distribution: Must=2, Should=13

| # | REQ | 约束项 | 确认值 | 优先级 | 状态 | 来源 |
|---|-----|--------|--------|--------|------|------|
| 1 | REQ-003 | 数据流特征 | ICAP 32-bit 粒度 | Should | confirmed | 继承自父模块 |
| 2 | REQ-004 | 延迟与吞吐 | 整体重配延迟 < 200ms | Should | confirmed | 继承自父模块 |
| 3 | REQ-009 | 可靠性 | CRC-32 校验 + 回滚机制 | Must | confirmed | 继承自父模块 |
| 4 | REQ-030 | 比特流压缩/解压 | 先支持 LZ4，Zlib 可选 | Should | confirmed | 继承自父模块 |
| 5 | REQ-031 | 传输-校验-写入流水线 | 三阶段重叠，校验段与传输段并行 | Should | confirmed | 继承自父模块 |
| 6 | REQ-032 | 比特流版本管理 | 记录版本号，支持回退 | Should | confirmed | 继承自父模块 |
| 7 | REQ-033 | 错误注入接口 | APB 可注入 CRC 错误 | Should | confirmed | 继承自父模块 |
| 8 | LREQ-001 | 比特流格式校验 | 校验格式合法性（魔数/版本/长度） | Must | confirmed | 头脑风暴新增 |
| 9 | LREQ-002 | CRC 流式计算 | 边接收边计算，不等待完整比特流 | Should | confirmed | 头脑风暴新增 |
| 10 | LREQ-003 | 解压流水线 | CRC 校验与 LZ4 解压重叠执行 | Should | confirmed | 头脑风暴新增 |
| 11 | LREQ-004 | 回滚版本选择 | 回滚时可选择目标版本 | Should | confirmed | 头脑风暴新增 |
| 12 | LREQ-005 | 压缩算法自动检测 | 头部算法标识字段（0x00=未压缩，0x01=LZ4） | Should | confirmed | 用户确认 |
| 13 | LREQ-006 | 解压结果校验 | 解压后二次校验数据完整性 | Should | confirmed | 头脑风暴新增 |
| 14 | LREQ-007 | 校验统计寄存器 | 校验通过/失败/回滚次数统计 | Should | confirmed | 头脑风暴新增 |
| 15 | LREQ-008 | 错误详情日志 | 记录最近 N 次校验错误详细信息 | Should | confirmed | 头脑风暴新增 |

### 优先级统计

| 优先级 | 数量 | REQ 编号 |
|--------|------|----------|
| Must | 2 | REQ-009, LREQ-001 |
| Should | 13 | REQ-003, REQ-004, REQ-030, REQ-031, REQ-032, REQ-033, LREQ-002~008 |

### 冻结规则

**冻结条件**：用户确认本汇总表后自动冻结
**冻结范围**：15 项 REQ（7 继承 + 8 本地）

## [PHASE-END] stageC phase2
